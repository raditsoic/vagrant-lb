# Vagrant state: machine IDs, box metadata, and per-VM SSH private keys.
.vagrant/

# Registry auth material and tunnel token (generated by registry/up.sh).
registry/htpasswd
registry/.env

# Ansible retry files (older ansible-core versions)
*.retry
