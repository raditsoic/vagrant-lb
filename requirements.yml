---
# Ansible collections the playbook needs beyond ansible-core.
# Install:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.docker
  - name: community.postgresql
