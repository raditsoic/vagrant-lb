[Unit]
Description=tiket demo app (gunicorn + PostgreSQL)
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/opt/tiket
ExecStart=/usr/bin/gunicorn --workers 2 --bind 127.0.0.1:8000 app:app
Restart=on-failure

[Install]
WantedBy=multi-user.target
